'use client'

// Re-export from the context provider for backwards compatibility
export type { HomeSettings, TagSection } from '@/context/HomeSettingsProvider'
export { useHomeSettings } from '@/context/HomeSettingsProvider'